
 <% include partials/header %>
      
    <%blogs.forEach((blog)=> {%>
        <div class="show-container">
            <h2><%=blog.title%></h2>
            <div><%=blog.created.toString().substring(4,15)%></div>
            <div><%=blog.body.substring(0, 40)%></div>
            <div class="read-more"><a href='/blogs/<%=blog._id%>'>Read more</a></div>
        </div>
   <% })%>

   <% include partials/footer %>


